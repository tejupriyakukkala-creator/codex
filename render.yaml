services:
  # Option 1: Fast Global Static Site (Free Tier)
  - type: web
    name: medtwin-ai
    env: static
    buildCommand: npm run build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
